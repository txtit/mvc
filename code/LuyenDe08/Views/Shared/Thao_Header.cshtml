@model IEnumerable<LuyenDe08.Models.PhanLoaiPhu>


<!-- Close Icon -->
<div class="nav-close">
    <i class="fa fa-close" aria-hidden="true"></i>
</div>
<!-- Logo -->
<div class="logo">
    <a href="index.html"><img src="img/core-img/logo.png" alt=""></a>
</div>
<!-- Amado Nav -->
<nav class="amado-nav">


    @*<ul>
            @foreach (var item in Model)
            {
                <li id="@item.MaPhanLoai" class="category-item" ><a href="#">@item.TenPhanLoaiPhu</a></li>

            }

        </ul>*@

    <ul>
        @foreach (var item in Model)
        {
            <li class="category-item" data-catid="@item.MaPhanLoai"><a href="#">@item.TenPhanLoaiPhu</a></li>
        }
    </ul>



</nav>
<!-- Button Group -->
<div class="amado-btn-group mt-30 mb-100">
    <a href="#" class="btn amado-btn mb-15">TRẦN XUÂN THAO</a>
    <a href="#" class="btn amado-btn active">CQ.62.CNTT</a>
</div>
<!-- Cart Menu -->
<div class="cart-fav-search mb-100">
    <a href="cart.html" class="cart-nav"><img src="img/core-img/cart.png" alt=""> Cart <span>(0)</span></a>
    <a href="#" class="fav-nav"><img src="img/core-img/favorites.png" alt=""> Favourite</a>
    <a href="#" class="search-nav"><img src="img/core-img/search.png" alt=""> Search</a>
</div>
<!-- Social Button -->
<div class="social-info d-flex justify-content-between">
    <a href="#"><i class="fa fa-pinterest" aria-hidden="true"></i></a>
    <a href="#"><i class="fa fa-instagram" aria-hidden="true"></i></a>
    <a href="#"><i class="fa fa-facebook" aria-hidden="true"></i></a>
    <a href="#"><i class="fa fa-twitter" aria-hidden="true"></i></a>
</div>
<script src="~/Scripts/js/jquery/jquery-2.2.4.min.js"></script>
<script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>

<script>
    @*$(document).ready(function () {
         $("li").on('click',function () {
        var id = $(this).attr("id");
        $.ajax({
            url: '@Url.Action("LoadProductByCatId","BaiThi")',
            type: 'GET',
            data: { maphanloai: id },
            success: function (response) {
                $("div.main-content-wrapper.d-flex.clearfix").html(response);
            }
        });
    });
    });*@
    $(document).ready(function () {
        $('.category-item').on('click', function (e) {
            e.preventDefault(); // Ngăn chặn hành động mặc định của thẻ 'a'

            var catId = $(this).data('catid');

            $.ajax({
                url: '/BaiThi/LoadProductByCatId',
                type: 'GET',
                data: { catId: catId },
                success: function (response) {
                    console.log("AJAX success:", response);
                    $("div.main-content-wrapper.d-flex.clearfix").html(response);
                },
                error: function () {
                    alert('Đã có lỗi xảy ra khi gửi yêu cầu.');
                }
            });
        });
    });


</script>